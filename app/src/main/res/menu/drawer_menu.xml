<!-- res/menu/sidebar_menu.xml -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Section: Navigation -->
    <group android:id="@+id/group_navigation" android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:title="Home"
            android:icon="@drawable/ic_distance"
            app:showAsAction="ifRoom"
            android:titleTextColor="?attr/colorOnSurface" />

        <item
            android:id="@+id/nav_compass"
            android:title="Compass"
            android:icon="@drawable/ic_compass"
            app:showAsAction="ifRoom"
            android:titleTextColor="?attr/colorOnSurface" />

        <item
            android:id="@+id/nav_asteroid_list"
            android:title="Asteroid List"
            android:icon="@drawable/ic_list"
            app:showAsAction="ifRoom"
            android:titleTextColor="?attr/colorOnSurface" />

        <item
            android:id="@+id/nav_mqtt"
            android:title="Maps"
            android:icon="@drawable/ic_maps"
            app:showAsAction="ifRoom"
            android:titleTextColor="?attr/colorOnSurface" />

        <item
            android:id="@+id/nav_add_shelter"
            android:title="Add Shelter"
            android:icon="@drawable/ic_bunker"
            app:showAsAction="ifRoom"
            android:titleTextColor="?attr/colorOnSurface" />
    </group>

    <!-- Section: Accessibility -->
    <group android:id="@+id/group_accessibility">
        <item
            android:id="@+id/switch_high_contrast"
            android:title="High Contrast"
            app:actionViewClass="android.widget.Switch"
            app:showAsAction="never"
            android:titleTextColor="?attr/colorOnSurface" />
    </group>

</menu>
